//#00 SINTAXIS, VARIABLES, TIPOS DE DATOS Y HOLA MUNDO
/*
  EJERCICIO:
  - Crea un comentario en el código y coloca la URL del sitio web oficial del
    lenguaje de programación que has seleccionado. ✅
  - Representa las diferentes sintaxis que existen de crear comentarios
    en el lenguaje (en una línea, varias...). ✅
  - Crea una variable (y una constante si el lenguaje lo soporta).✅
  - Crea variables representando todos los tipos de datos primitivos 
    del lenguaje (cadenas de texto, enteros, booleanos...). ✅
  - Imprime por terminal el texto: "¡Hola, [y el nombre de tu lenguaje]!"✅*/

// Este es el sitio web oficial de JavaScript: https://developer.mozilla.org/es/docs/Web/JavaScript

// Comentario en una línea

/* 
Comentario 
en varias
líneas 
*/

// Esta es una variable
let lenguaje = "JavaScript";
var variable = 100;

// Esta es una constante
const constante = 1;   

// Estos son todos los tipos de datos primitivos
let nombre = "ervato"; // Cadena de texto (String)
let edad = 23; // Número entero
let altura = 1.75; // Número decimal
let esAprendiz = true // Booleano, puede ser true o false
let ciudad; // Undefined, porque no tiene valor asignado todavía
let resultado = null; //Null, se usa para indicar que no hay un valor
let id = Symbol("identificador"); // Symbol, es único
let numeroGrande = BigInt(1256235556462645); // BigInt, para números grandes

//Así se imprime por terminal un texto
console.log("¡Hola, JavaScript!"); 
